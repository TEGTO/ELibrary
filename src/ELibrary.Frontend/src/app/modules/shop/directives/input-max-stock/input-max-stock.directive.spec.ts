import { InputMaxStockDirective } from './input-max-stock.directive';

describe('InputMaxStockDirective', () => {
  it('should create an instance', () => {
    const directive = new InputMaxStockDirective();
    expect(directive).toBeTruthy();
  });
});
