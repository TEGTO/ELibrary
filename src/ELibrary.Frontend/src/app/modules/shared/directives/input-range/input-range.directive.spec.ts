import { InputRangeDirective } from './input-range.directive';

describe('InputRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new InputRangeDirective();
    expect(directive).toBeTruthy();
  });
});
