version: '3.4'

services:
  elibrary-db:
    environment:
        - POSTGRES_DB=${POSTGRES_ELIBRARY_DB}
        - POSTGRES_USER=${POSTGRES_ELIBRARY_USER}
        - POSTGRES_PASSWORD=${POSTGRES_ELIBRARY_PASSWORD}